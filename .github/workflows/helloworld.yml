name : "Hello World Workflow"
on : 
  push :
    branches :
      - master
jobs :
  hello_world :
    runs-on : ubuntu-latest
    steps :
      - name : "Hello World Step"
        run : echo "Hello, World!"